{
  "name": "movie_entries",
  "version": "1.0.0",
  "description": "A Node.js app that presents users with a form containing three fields and sets up a route using the MongoDB driver to insert a movie document into MongoDB",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Liban Mohamed",
  "license": "ISC",
  "dependencies": {
    "assert": "^1.4.0",
    "body-parser": "^1.15.1",
    "consolidate": "^0.14.1",
    "express": "^4.13.4",
    "mongodb": "^2.1.20",
    "nunjucks": "^2.4.2"
  }
}
